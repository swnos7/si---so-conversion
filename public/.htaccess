<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Route all legacy traffic to the construction company website.
  RewriteRule ^ https://techniqueartconstruction.site/ [R=301,L]
</IfModule>

<IfModule mod_headers.c>
  Header set X-Powered-By "Hostinger Horizons"
</IfModule>
